upstream weather_backend {
    server web:8000;
}

server {
    listen 80;
    server_name weather-forecast.click;
    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name weather-forecast.click;

    ssl_certificate     /etc/letsencrypt/live/weather-forecast.click/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/weather-forecast.click/privkey.pem;

    location / {
        limit_req zone=req_limit;
        limit_conn conn_limit 5;

        proxy_pass http://weather_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
